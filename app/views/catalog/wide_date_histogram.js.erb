$("#modal-wide_date_histogram").find(".modal-content").html("<%= j (render 'wide_date_histogram') %>");
$("#modal-wide_date_histogram").modal();
$('#modal-wide_date_histogram')[0].classList.remove('hide');