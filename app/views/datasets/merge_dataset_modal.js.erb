$("#modal-merge-dataset").find(".modal-content").html("<%= j (render 'merge_dataset', locals: {dataset: @dataset, other_dataset: @other_dataset}) %>");
$("#modal-merge-dataset").modal();
$('#modal-merge-dataset')[0].classList.remove('hide');